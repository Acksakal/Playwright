{
  "name": "playwright",
  "version": "1.0.0",
  "description": "playing with playwright",
  "scripts": {
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui",
    "test:debug-logs": "DEBUG=* DEBUG_FILE=logs.txt npx playwright test"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.44.1",
    "@types/node": "^20.14.2"
  }
}
